# Copy to .env or export in your shell. Never commit real values.

# Optional for this lab: without a key the deterministic demonstration still
# runs; only the final real generation step is skipped.
# Create a key at https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# Optional. Defaults to gemini-3.8-flash
GEMINI_MODEL=
